# fwjs
## Correr react 
+ desde la carpeta quiz
    - npm i 
    - npm run dev

## Apuntes 
+ Esto 
    - { showResults ? resultsMode : questionMode } 

+ Es lo mismo que esto 
    - if (showResults == true){
        resultMode
    }
    else{
        questionMode
    }

